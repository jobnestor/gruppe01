#if !defined(__LAG_H)
#define __LAG_H
#include "const.h"

class Lag {
private:
	char* navn;
	char* adresse;
	int spillerNr[SPILLERLAG];		// Array med de unike nr til spillerne
public:
	// lag inmaten
};

#endif
